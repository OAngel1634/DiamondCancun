# html/.htaccess
RewriteEngine On

# Redirección a HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# No bloquear la carpeta admin
RewriteRule ^admin/ - [L]